# 인증 구현 설명서

> 아래 내용은 코드 기준 설명입니다. 실제 통과 여부는 브라우저에서 테스트한 결과로 최종 수정하세요.

## ① 무엇으로 붙였나

- 방식: 라이브러리
- 서버: `@simplewebauthn/server`
- 브라우저: `@simplewebauthn/browser`
- 세션: 서버가 직접 보관하는 불투명 랜덤 세션 토큰 + `httpOnly` 쿠키

## ② 왜 그걸 골랐나

WebAuthn의 challenge, 공개키, authenticator 응답을 직접 파싱해 검증하는 대신 표준 WebAuthn 검증 라이브러리를 사용해 등록·로그인 흐름을 분리했습니다. 과제에서 요구하는 challenge 검증과 서명 검증을 구현 코드에서 명확하게 확인하기 위한 선택입니다.

## ③ 어디를 어떻게 고쳤나

| 흐름 | 소스 위치 |
|---|---|
| 공개 소개 | `public/index.html` |
| 등록 | `public/app.js` → `/api/register/options` → `/api/register/verify` |
| 로그인 | `public/app.js` → `/api/login/options` → `/api/login/verify` |
| 로그아웃 | `public/app.js` → `/api/logout` |
| 비공개 자료 | `public/app.js` → `GET /api/private/items` + `requireAuth` |
| 패스키 관리 | `public/app.js` → `/api/passkeys` |
| 데이터 저장 | `src/db.js` |

### challenge

`src/db.js`의 `setChallenge()`에서 발급 시각과 5분의 `expiresAt`을 저장하고, `consumeChallenge()`에서 꺼내면서 즉시 삭제합니다.

### 공개키

등록 검증 성공 후 `registrationInfo.credential.publicKey`만 `credentials`에 저장합니다. 개인키는 서버 코드가 생성하거나 저장하지 않습니다.

### 비공개 경계

처음 HTML에는 비공개 항목 내용이 들어 있지 않습니다. 실제 비공개 내용은 로그인 성공 후 `GET /api/private/items`로 요청하며 `requireAuth`를 통과한 세션의 사용자만 받을 수 있습니다.

### 세션

로그인 성공 후 `crypto.randomBytes(32)`로 만든 랜덤 토큰을 `httpOnly` 쿠키로 발급하고, 서버의 `sessions` 저장값과 대조합니다. 로그아웃 시 서버 세션을 삭제하므로 같은 쿠키를 다시 보내도 인증되지 않습니다.

## ④ 안 열리는 것을 확인한 기록

> 아래는 자리표시자입니다. 실제 테스트 결과와 캡처를 넣으세요.

### 로그인 없이 비공개 자료 열기
- 예상 거절: `GET /api/private/items` → `401`
- 실제 결과: ____________________

### 잘못된 서명/다른 계정의 패스키
- 예상 거절: `POST /api/login/verify` → `401` 또는 `403`
- 실제 결과: ____________________

### 이미 사용한 challenge 재사용
- 예상 거절: 동일 verify 요청 재전송 → `400`
- 실제 결과: ____________________

### 패스키 삭제 뒤 로그인
- 남은 패스키: 로그인 성공
- 삭제한 패스키: 후보에서 제외 또는 서버에서 거절
- 실제 결과: ____________________

### 계정 간 교차 접근
- A 세션으로 B의 식별자를 넣어도 서버가 세션 사용자 A만 기준으로 자료를 반환하는지 확인
- 다른 계정 패스키로 로그인 verify 시도 시 계정 소유권 검사에 걸리는지 확인
- 실제 결과: ____________________

## ⑤ AI와 나

1. AI에게 맡긴 일: WebAuthn 라이브러리를 이용한 등록/로그인 흐름과 테스트 문서의 초안을 작성했습니다.
2. 내가 직접 판단한 일: 1번 과제 원본 저장소와 8번 과제 저장소를 분리하고, 공개 영역은 포트폴리오 내용으로 유지하면서 비공개 영역만 패스키로 보호하는 구조를 선택했습니다.
3. AI 제안을 따르지 않은 일: JWT 대신 서버가 직접 무효화할 수 있는 세션 방식을 유지했고, 과제용 JSON 저장소의 배포 한계도 별도로 표시했습니다.

## ⑥ 아직 못 막은 것

- 현재 `data/db.json`은 과제용 파일 저장소라서 서버/호스팅 환경에 따라 영구성이 보장되지 않을 수 있습니다. 실제 운영 환경에서는 영구 DB로 교체해야 합니다.
- 현재 앱에는 전문적인 계정 복구/레이트리밋/감사로그 같은 운영 수준 기능이 없습니다. 과제 범위 밖의 항목으로 구분합니다.

# 짧은 확인 방법

1. 배포/로컬 주소에 접속하면 공개 포트폴리오가 먼저 보입니다.
2. 아래 `🔒 나만의 자리`에서 아이디를 입력하고 패스키를 등록하거나 로그인합니다.
3. 로그인 후에만 비공개 항목과 등록된 패스키 목록이 나타납니다.
4. 로그아웃하면 다시 비공개 API를 사용할 수 없습니다.
