# 서버 포트
PORT=3000

# WebAuthn Relying Party 설정
# 로컬: RP_ID=localhost / ORIGIN=http://localhost:3000
# 배포: RP_ID=서비스도메인 / ORIGIN=https://서비스도메인
RP_ID=localhost
RP_NAME=Security Portfolio Passkey
ORIGIN=http://localhost:3000

# 배포 환경에서는 아래도 함께 설정
NODE_ENV=development
